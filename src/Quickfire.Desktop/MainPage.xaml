<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Quickfire.Desktop.MainPage"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <ContentPage.Resources>
        <Style TargetType="Entry">
            <Setter Property="BackgroundColor" Value="White" />
            <Setter Property="TextColor" Value="#111827" />
            <Setter Property="PlaceholderColor" Value="#9CA3AF" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="HeightRequest" Value="44" />
            <Setter Property="HorizontalOptions" Value="Fill" />
            <Setter Property="VerticalOptions" Value="Center" />
            <Setter Property="Margin" Value="0" />
        </Style>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">
        <HorizontalStackLayout Padding="8" Spacing="12">
            <Button Text="Pair helper" Clicked="OnPairHelperClicked" />
            <Button Text="Forget helper" Clicked="OnForgetHelperClicked" />
            <Button Text="Use local server" Clicked="OnUseLocalServerClicked" />
            <Label x:Name="BridgeStatus" Text="Helper not paired" VerticalOptions="Center" />
            <Label x:Name="ServerAddress" VerticalOptions="Center" LineBreakMode="TailTruncation" />
        </HorizontalStackLayout>
        <WebView x:Name="AppWebView"
                 Grid.Row="1"
                 IsVisible="False"
                 HorizontalOptions="Fill"
                 VerticalOptions="Fill" />

        <Grid x:Name="LoadingOverlay"
              Grid.Row="1"
              BackgroundColor="#F0F0F0"
              Opacity="0.96"
              HorizontalOptions="Fill"
              VerticalOptions="Fill">
            <StackLayout HorizontalOptions="Center"
                         VerticalOptions="Center"
                         Spacing="12">
                <ActivityIndicator x:Name="LoadingIndicator"
                                   IsRunning="True"
                                   HeightRequest="32"
                                   WidthRequest="32" />
                <Label x:Name="LoadingMessage"
                       Text="Starting Quickfire..."
                       HorizontalOptions="Center"
                       TextColor="#1F2937" />
                <Button x:Name="RetryButton"
                        Text="Retry"
                        IsVisible="False"
                        Clicked="OnRetryClicked"
                        HorizontalOptions="Center"
                        BackgroundColor="#2563EB"
                        TextColor="White" />
            </StackLayout>
        </Grid>
        <Grid x:Name="PairingOverlay" Grid.RowSpan="2" IsVisible="False" BackgroundColor="#EEFFFFFF" Padding="24">
            <VerticalStackLayout MaximumWidthRequest="520" VerticalOptions="Center" Spacing="12">
                <Label Text="Pair this desktop helper" FontSize="22" />
                <Label Text="Enter your server and choose Open server to sign in. Create an automation device credential in your Quickfire profile, then return here to save the pairing. Changing servers also changes this window." />
                <Entry x:Name="PairingServer" Placeholder="https://quickfire.example" />
                <Button Text="Open server" Clicked="OnOpenServerClicked" />
                <Entry x:Name="PairingToken" Placeholder="Device credential" IsPassword="True" />
                <Label x:Name="PairingError" TextColor="DarkRed" />
                <Button Text="Save pairing" Clicked="OnSavePairingClicked" />
                <Button Text="Cancel" Clicked="OnCancelPairingClicked" />
            </VerticalStackLayout>
        </Grid>
    </Grid>

</ContentPage>
